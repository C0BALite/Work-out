using UnityEngine;

/// <summary>Marks a saved UI hierarchy. Layout is edited in the scene or prefab, never rebuilt at startup.</summary>
[DisallowMultipleComponent]
public sealed class WorkOutAuthoredUI : MonoBehaviour { }
